# 🧠 Raspberry Pi Pico + micro-ROS + RFID Reader (MFRC522)

## 🧩 micro-ROS 통신 구조
```css
[Pico] <——serial——> [micro-ROS Agent] <——DDS——> [ROS 2 (스카우트 미니)]
```

| 구성 요소                 | 역할                                           | 위치                       |
| --------------------- | -------------------------------------------- | ------------------------ |
| **Raspberry Pi Pico** | `micro-ROS client` (Publisher/Subscriber 동작) | Pico                     |
| **micro-ROS Agent**   | Pico ↔ ROS 2 사이의 **브리지(bridge)** 역할          | **호스트 측(스카우트 미니 or PC)** |
| **ROS 2 System**      | 토픽, 서비스, 액션 관리 (DDS 네트워크 관리)                 | **스카우트 미니**              |


## ⚙️ 튜토리얼

### 1️⃣ 환경 준비
**✅ 필수 항목**
- Raspberry Pi Pico
- Ubuntu PC (ROS 2 환경 포함)
- USB 케이블 (데이터 전송용)
- MFRC522 RFID 모듈, LED
- 점퍼 케이블
- Thonny(MicroPython 업로드용)

### 2️⃣ MicroPython + micro-ROS 설치

**① Pico에 MicroPython 설치**
1. [micro_ros_raspberrypi_pico github 페이지](https://github.com/micro-ROS/micro_ros_raspberrypi_pico_sdk/tree/humble) 참고
2. Pico를 **BOOTSEL 버튼 누른 상태로 PC 연결**
3. 새로 뜬 드라이브에 .uf2 파일 복사 → 자동 재부팅

**[주의] ros2 버전을 github branch에서 꼭 확인하기**

**② Thonny에서 연결 확인**
```python-repl
MicroPython (Raspberry Pi Pico)
>>> print("Hello Pico")

```
정상 출력되면 연결 완료 ✅


### 3️⃣ micro-ROS 빌드 및 플래싱 (호스트 PC 측)

**① micro-ROS 에이전트 설치**
```
sudo apt install ros-humble-micro-ros-agent
```

**② pico SDK 및 micro-ROS 라이브러리 클론 && 예제 빌드**
```
git clone -b humble https://github.com/micro-ROS/micro_ros_raspberrypi_pico_sdk
cd micro_ros_raspberrypi_pico_sdk
mkdir build
cd build
cmake ..
make

```

### 4️⃣ RFID (MFRC522) 연결
1. [raspberrypi_pico RFID github 페이지](https://www.tomshardware.com/raspberry-pi/raspberry-pi-pico/how-to-use-an-rfid-reader-with-a-raspberry-pi-pico) 참고
  - **[mfrc522 패키지 링크](https://github.com/danjperron/micropython-mfrc522/blob/master/mfrc522.py)**


### 5️⃣ RFID 인식 프로그램 실행

### 6️⃣ 실행
1. Thonny에서 main.py 업로드
2. REPL 실행 → 카드 인식 결과 확인
3. 지정된 카드일 경우 LED 점등


### 7️⃣ micro-ROS 통신 테스트

**Pico 쪽에서 `micro_ros_publisher_example` 실행**
**PC 쪽에서 Agent 실행**
```
ros2 run micro_ros_agent micro_ros_agent serial --dev /dev/ttyACM0
```

**ROS 2 토픽 확인**
```
ros2 topic list
ros2 topic echo /micro_ros/publisher
```
